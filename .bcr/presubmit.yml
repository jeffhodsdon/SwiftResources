matrix:
  platform: [macos_arm64, macos]
  bazel: [7.x, 8.x]
tasks:
  verify_targets:
    name: Verify targets on ${{ platform }}
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@rules_swift_resources//..."
    test_targets:
      - "@rules_swift_resources//..."
